Imagenes para lado B por método de medición por presión
♥ PBM_B.png, sería la figura estática sin darle run al modelo para el método de medición por presión
♥ PBM_B_Agitador, sería la figura animada cuando únicamente la variable de salida agitacion Lado B "mixVelocityR106" sea diferente de 0.
♥ PBM_B_inyector, Sería la figura animada cuando únicamnete la variable de salida caudal de inyección "caudalSideB" es diferente de 0.
♥ PBM_B_agitador+inyector, sería la figurada animada cuando la variable de salida agitación lado B "mixVelocityR106" y el caudal de inyección lado B son diferentes de 0 "caudalSideB".
♥ Si agitación y caudal de inyección son cero, se mostraría la figura estática.

Imagenes para la B por método de medición por volumen desplazado
♥ PBM_B_piscina. Sería la figura estática sin darle run al modelo.
♥ PBM_B_inyector+piscina, sería la figura animada que se muestra cuando el caudal de inyección "caudalSideB" es diferente o mayor a 0.
♥ PBM_B_Agitador+piscina, sería la figura animada que se muestra cuando la agitación "mixVelocityR106" es diferente o mayor a 0. 
♥ PBM_B_Agitador + inyector + Piscina, Sería la figurada animada que se muestra cuando el caudal de inyección "caudalSideB" y la agitación "mixVelocityR106" son diferentes de 0.
♥ PBM_B_piscina. Se mostraría la figura estática en caso tal de que agitación y caudal de inyección sean 0.