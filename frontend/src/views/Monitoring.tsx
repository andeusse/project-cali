import { Container } from '@mui/material';
import React from 'react';

type Props = {};

const Monitoring = (props: Props) => {
  return <Container maxWidth="xl">Monitoring</Container>;
};

export default Monitoring;
